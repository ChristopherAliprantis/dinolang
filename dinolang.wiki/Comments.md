
**Comments**

start with // and with ;

must be per line you can't have "print(:invalid code:) //dasdd;", but you can have "print(:invalid code:); //dasdd;"

**Multi-Line Comment Example**

//print(var)
print(2);

NOTE: the multi-line comment is code because multi-line comments are commonly used to comment out blocks of code