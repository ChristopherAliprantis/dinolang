#func Derr(message, back, after)a;
    back = +(back, 3);
    printnnl(+(message, COMMA, : :, :Line :,Line(back)));
    after = +(: :, after);
    print(after);
    Exit(1);
    return();
#endfunc;

#func print(in)a;
    in = +(in, NL);
    printnnl(in);
    return();
#endfunc;