#func Derr(message, after)a;
    back = +(back, 3);
    printnnl(+(message, COMMA, : :, :Line:, DLine);
    after = +(: :, after);
    print(after);
    Exit(1);
    return();
#endfunc;

#func print(in)a;
    in = +(in, NL);
    printnnl(in);
    return();
#endfunc;