#func Derr(message, nnl);
    #if (!=(nnl, true));print(+(message, COMMA, : :, :Line :,LINE); #endif;
    #if (==(nnl, true));printnnl(+(message), COMMA, : :, :Line :,LINE); #endif;
    Exit(1);
#endfunc;