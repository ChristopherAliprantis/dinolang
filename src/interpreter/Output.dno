#func Derr(message, nnl)a;
    #if (==(nnl, false));print(+(message, COMMA, : :, :Line :,Line(5))); #endif;
    #if (==(nnl, true));printnnl(+(message, COMMA, : :, :Line :,Line(8))); #endif;
    Exit(1);
    return();
#endfunc;