#func ispos(n); #if (<(0, n)); return(true); #endif; return(false); #endfunc;
#func isneg(n); #if (>(0, n)); return(true); #endif; return(false); #endfunc;
#func isneut(n); #if (==(n, 0)); return(true); #endif; return(false); #endfunc;

#func abs(n);
    #if (<(-1, n));
        return(n);
    #endif;
    abs = *(n, -1);
    return(abs);
#endfunc;