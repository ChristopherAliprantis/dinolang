declaration example:

#if (==(2, 2));

ending example:

#endif;

can go in functions, outside functions, in loops, and outside functions, but not in ifs<br>
can make a function return stuff if in a function<br>

**Example**

NOTE: still put a tab or 4 spaces inside your code blacks it isn't rendering here because of the markdown renderer

#if (<(var, 9));<br>
    print(:9 is greater than var:);<br>
#endif<br>
    